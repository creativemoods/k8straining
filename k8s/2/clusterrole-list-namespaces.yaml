# Cluster Role lsns
apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRole
metadata:
  name: clusterrole-lsns-<prenom>
rules:
  - apiGroups: ['']
    resources: ['namespaces']
    verbs: ['get', 'list']
